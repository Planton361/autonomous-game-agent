# Session Report — M-006

Status: done
Milestone: M-006 — Cortex Output Evidence Scope Gate
Model profile: standard
Branch: `codex/m006-cortex-evidence-scope-gate`
Base commit: `1c30634d933f41d9b2e6901e392b442910b7e6c9`
Commit: pending-at-report-write
Pull Request: pending-at-report-write

## Implemented

- Added a private Cortex context/output evidence-scope gate after `PlannerOutput` parsing and before accepting the selected skill.
- Allowed only non-empty IDs explicitly carried by the current observation evidence list or retrieved facts, hypotheses, and recent skill outcomes.
- Rejected entire outputs with evidence IDs outside the supplied `CortexContext` using `PlannerOutputError` and sorted offending IDs.
- Kept `screenshot_id` descriptive: it is not evidence authority without membership in observation `evidence_ids`.

## Acceptance Criteria

| Criterion | Result |
| --- | --- |
| Current observation evidence accepted | pass |
| Retrieved fact and hypothesis support accepted | pass |
| Fabricated fact, hypothesis, and memory-update evidence rejected | pass |
| Mixed valid and invalid evidence rejects the full output | pass |
| Evidence-free hypothesis remains valid | pass |
| `screenshot_id` alone grants no evidence authority | pass |
| Existing direct-control, hidden-state, and skill checks remain covered | pass |
| Valid and evidence-invalid outputs make one fake LLM request | pass |

## Verification

| Command | Result |
| --- | --- |
| `uv run pytest tests/test_cortex.py tests/test_planner_context.py tests/test_planner_output.py tests/test_llm_client.py tests/test_task_manager.py` | 90 passed (focused baseline) |
| `uv run pytest tests/test_cortex.py tests/test_planner_context.py tests/test_planner_output.py tests/test_llm_client.py tests/test_task_manager.py tests/test_task_executor.py` | 106 passed |
| Cortex dependency and evidence-scope searches | no Bridge/Manager/runtime dependency; scope logic manually inspected |
| `uv run pytest` | 1285 passed |
| `uv run ruff check .` | passed |
| `uv run ruff format --check .` | 188 files already formatted |
| `git diff --check` | passed |
| `uv run fh-agent --help` | passed; help output only, no game, input, or LLM activity |

## Baseline Issues

None.

## Deviations / Assumptions

- Independent read-only `milestone_reviewer`: No issue; final decision `approve`.

## Residual Risks / Blockers

- No implementation blocker. A2 commit, push, and Draft-PR handoff remain pending at report write.

## Next Necessary Decision

User review and merge decision for the M-006 Draft PR after publication handoff.

## Scope

- No Manager, Bridge, Observation, memory, input/runtime, schema, canonical, pilot, or dependency change.
- Tests used `FakeLLMClient` only; no networked LLM call, live game, or real input occurred.
