# Session Report — M-008

Status: done
Milestone: M-008 — Grounding Request Derivation
Model profile: standard
Branch: `codex/m008-grounding-request-derivation`
Base commit: `0e3e38855f188854f8efc3f35e28b5784a794689`
Commit: pending-at-report-write
Pull Request: pending-at-report-write

## Implemented

- Added the pure `build_grounding_request` composition boundary.
- Targetless skills return `None`; target-required skills preserve the exact selected skill and semantic goal.
- Request scope is an ordered, deduplicated tuple of non-empty `Observation.evidence_ids` only.
- Added compatibility tests showing that the existing GroundingService, not the builder, adds candidate evidence and owns `insufficient_evidence`.

## Acceptance Criteria

| Criterion | Result |
| --- | --- |
| Targetless skills return `None` | pass |
| Target-required requests preserve selected skill and semantic goal | pass |
| Scope uses only ordered, deduplicated current visible evidence | pass |
| Historical planner, descriptive screenshot, and candidate evidence are excluded | pass |
| Empty scope reaches existing `insufficient_evidence` authority | pass |
| Production builder has no Cortex, submission, execution, bridge, game, or memory dependency | pass |

## Verification

| Command | Result |
| --- | --- |
| `uv run pytest tests/test_grounding.py tests/test_task_manager.py tests/test_cortex_submission.py tests/test_cortex.py tests/test_planner_output.py` | 97 passed (focused baseline) |
| `uv run pytest tests/test_grounding_request_builder.py tests/test_grounding.py tests/test_task_manager.py tests/test_cortex_submission.py tests/test_cortex.py tests/test_planner_output.py` | 105 passed |
| M-008 and existing-grounding dependency searches | no forbidden matches |
| `uv run pytest` | 1302 passed |
| `uv run ruff check .` | passed |
| `uv run ruff format --check .` | 192 files already formatted |
| `git diff --check` | passed |
| `uv run fh-agent --help` | passed; help output only, no game, input, bridge, or LLM activity |

## Baseline Issues

None.

## Deviations / Assumptions

- Independent read-only `milestone_reviewer`: No issue; final decision `approve`.

## Residual Risks / Blockers

- No implementation blocker. A2 commit, push, and Draft-PR handoff remain pending at report write.

## Next Necessary Decision

User review and merge decision for the M-008 Draft PR after publication handoff.

## Scope

- No automatic grounding, Manager submission, task execution, Cortex, bridge, Observation, Body, Verifier, memory, schema, canonical, pilot, or dependency change.
- GroundingService was invoked only in deterministic compatibility tests; production code does not invoke it.
- No live LLM, bridge transport, game activity, or real input occurred.
