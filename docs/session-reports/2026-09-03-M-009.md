# Session Report — M-009

Status: done
Milestone: M-009 — Grounded Cortex Task Submission
Model profile: standard
Branch: `codex/m009-grounded-cortex-task-submission`
Base commit: `4b425d4e88a4364a82615248fc1d63e88f6572ec`
Commit: pending-at-report-write
Pull Request: pending-at-report-write

## Implemented

- Added `GroundedCortexTaskSubmitter`, a focused composition boundary from planning through grounding to Manager submission.
- Reused the M-007 planner protocol and the M-008 request builder without rewriting planner intent or request scope.
- Targetless submission skips GroundingService; target-required submission invokes it once and forwards its exact result to Manager validation.
- Added deterministic Manager-rejection, current-evidence, and synchronized bridge-assisted composition coverage.

## Acceptance Criteria

| Criterion | Result |
| --- | --- |
| Active Manager capability subset constrains planning | pass |
| Exact planner output, request, grounding result, and provenance IDs are forwarded | pass |
| Targetless task skips grounding and remains `PENDING` | pass |
| Target-required request uses current Observation evidence only | pass |
| Grounding failures and incompatible targets reach Manager rejection | pass |
| Ambiguous candidates are rejected without guessing | pass |
| Synchronized bridge-assisted visible object reaches one pending Manager task | pass |
| No task start, Body execution, input, or verifier path | pass |

## Verification

| Command | Result |
| --- | --- |
| `uv run pytest tests/test_cortex_submission.py tests/test_grounding_request_builder.py tests/test_grounding.py tests/test_task_manager.py tests/test_manager_orchestrator.py tests/test_cortex.py` | 112 passed (focused baseline) |
| `uv run pytest tests/test_grounded_cortex_submission.py tests/test_cortex_submission.py tests/test_grounding_request_builder.py tests/test_grounding.py tests/test_task_manager.py tests/test_manager_orchestrator.py tests/test_cortex.py tests/test_bridge_observation_source.py` | 150 passed |
| M-009 source dependency search | no forbidden matches |
| `uv run pytest` | 1310 passed |
| `uv run ruff check .` | passed |
| `uv run ruff format --check .` | 194 files already formatted |
| `git diff --check` | passed |
| `uv run fh-agent --help` | passed; help output only, no game, input, bridge, or LLM activity |

## Baseline Issues

None.

## Deviations / Assumptions

- Independent read-only `milestone_reviewer`: No issue; final decision `approve`.

## Residual Risks / Blockers

- No implementation blocker. A2 commit, push, and Draft-PR handoff remain pending at report write.

## Next Necessary Decision

User review and merge decision for the M-009 Draft PR after publication handoff.

## Scope

- No existing Cortex, submission, request-builder, GroundingService, TaskManager, bridge, Observation, Body, Verifier, memory, schema, canonical, pilot, or dependency change.
- No task was started. Tests used only deterministic planners, `FakeLLMClient`, and in-memory/test-local artifacts; no live LLM, bridge transport, game activity, or real input occurred.
