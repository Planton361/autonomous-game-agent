# Session Report — M-004

Status: `done`

Milestone: M-004 — Bridge-assisted Screenshot Evidence Synchronization
Model profile: `standard`
Branch: `codex/m004-bridge-screenshot-evidence-sync`
Base commit: `f9cbb52e67bd1475645116098154ddae58e10c9a`
Commit: `pending-at-report-write`
Pull Request: `pending-at-report-write`

## Implemented

- Added the read-only `BridgeScreenshotEvidenceLookup` port and `EventLogBridgeScreenshotEvidenceLookup`.
- Bridge-assisted sources now require a lookup at construction, but do not read it until a payload has passed the existing adapter and mode check.
- A bridge-assisted Observation now requires a non-empty payload screenshot ID exactly equal to the latest valid durable screenshot evidence ID for the source run.
- Missing, fabricated, and stale screenshot IDs raise `BridgeEvidenceSynchronizationError`; debug mode retains optional, ignored lookup behavior.
- The updated deterministic `SkillRunner` integration appends each test screenshot evidence event immediately before its corresponding raw payload and preserves before/action/after linkage.

## Verification

| Command / check | Result |
| --- | --- |
| Focused baseline: bridge source, event log, evidence, capture, sanitizer, server | PASS — 73 passed |
| Focused final: M-004 lookup/source plus bridge, runner, and input checks | PASS — 143 passed |
| Evidence-sync forbidden-dependency search | PASS — no forbidden control/runtime matches |
| Generic observation-port dependency search | PASS — no bridge matches |
| `uv run pytest` | PASS — 1269 passed |
| `uv run ruff check .` | PASS — All checks passed |
| `uv run ruff format --check .` | PASS — 186 files already formatted |
| `git diff --check` | PASS |
| `uv run fh-agent --help` | PASS — help only; no game launch or input |

## Synchronization evidence

Matching latest screenshot IDs return canonical Observations. Missing, fabricated, and stale IDs are rejected; the stale regression records `shot-1` then `shot-2` and rejects a payload claiming the historical `shot-1`. The synchronized SkillRunner test pulls two raw payloads, sends one `CONFIRM` through `DryRunInputBackend`, and preserves `shot-before` plus `shot-after` on both verifier and action evidence.

## Reviewer

Unavailable in this Codex session: the repo-local `milestone_reviewer` configuration has no callable reviewer tool exposed here. No files were edited by a reviewer.

## Scope

Changed files: `src/fh_agent/bridge/evidence_sync.py`, `src/fh_agent/bridge/observation_source.py`, `tests/test_bridge_evidence_sync.py`, `tests/test_bridge_observation_source.py`, the M-004 milestone, operational roadmap, and this report.

No canonical, pilot, memory implementation, CaptureSession, Observation schema, sanitizer, bridge-server, live-game, transport, dependency, evidence mutation, or semantic screenshot-validation changes occurred.

## Next necessary decision

Review and explicitly merge the M-004 Draft PR before cutting any later Phase-D milestone.
