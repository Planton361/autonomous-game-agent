# Session Report — M-002

Status: `done`

Milestone: M-002 — Canonical Bridge Run-Mode Contract
Model profile: `standard`
Branch: `codex/m002-bridge-run-mode-contract`
Base commit: `b2b53b963dd24dd077a1d18fde4db51afd281534`
Commit: `pending-at-report-write`
Pull Request: `pending-at-report-write`

## Implemented

- Defined the single bridge-specific `BridgeRunMode = Literal["bridge-assisted", "debug"]` at the sanitizer boundary.
- Reused that type for `SanitizedBridgePayload` and `BridgeReceipt`; the server no longer declares a duplicate mode type.
- Accepted `bridge-assisted` and `debug`; rejected `official`, `screen-only`, `networked-api-exploratory`, `contaminated`, and `training` through `InvalidBridgePayloadError`.
- Preserved visible-only sanitized output, receipt mode identity, Observation conversion, screenshot evidence, and deny-by-default field protection.

## Phase transition

Phase C — completed. Phase D — active.

## Verification

| Command / check | Result |
| --- | --- |
| Focused baseline: bridge sanitizer, server, observation integration | PASS — 35 passed |
| Focused final: plus firewall allowlist and import checks | PASS — 64 passed |
| Obsolete-mode/type search | PASS — one type definition; only legacy `official` rejection test remains |
| `uv run pytest` | PASS — 1231 passed (five M-002 tests added to the 1226-test base) |
| `uv run ruff check .` | PASS — All checks passed |
| `uv run ruff format --check .` | PASS — 182 files already formatted |
| `git diff --check` | PASS |
| `uv run fh-agent --help` | PASS — help only; no game launch or input |

## Reviewer

Unavailable in this Codex session: the repo-local `milestone_reviewer` configuration has no callable reviewer tool exposed here. No files were edited by a reviewer.

## Scope

Changed files: `src/fh_agent/bridge/sanitizer.py`, `src/fh_agent/bridge/bridge_server.py`, `tests/test_bridge_sanitizer.py`, `tests/test_bridge_server.py`, `tests/test_bridge_observation_integration.py`, the M-002 milestone, operational roadmap, and this report.

No canonical, pilot, dependency, lockfile, evaluator-manifest, JavaScript bridge, live-game, bridge-connection, or input changes occurred.

## Related out-of-scope drift

`src/fh_agent/evals/**` still contains historical manifest strings including `official_screen_only`, `debug_visible_bridge`, and `dry_run`. They were intentionally not changed; a later explicit review is required before any broader run-mode normalization.

## Next necessary decision

Review and explicitly merge the M-002 Draft PR before cutting any later Phase-D milestone.
