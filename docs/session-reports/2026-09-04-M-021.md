# Session Report — M-021 Request-Bound Bridge Snapshot Response

## Repository

- Actual base SHA: `ee24b41b2f0bc3b766924400ff6738ccdb1c3cc3`
- Worktree: `/home/anton/PycharmProjects/autonomous-game-agent-m021`
- Branch: `codex/m021-bridge-snapshot-response`
- Commit: published branch head (`Correlate bridge snapshots with requests`)
- Draft PR: [#23](https://github.com/Planton361/autonomous-game-agent/pull/23)

## Verification

- Focused baseline: `97 passed` (`test_bridge_snapshot_request`, M-019/M-020 bridge contract
  tests, sanitizer, observation/evidence synchronization).
- Focused final: `112 passed` (new response tests plus the focused bridge suites).
- Full validation: `1442 passed`; `uv run ruff check .`; `uv run ruff format --check .`;
  `git diff --check`; and `uv run fh-agent --help` all passed.
- Review: approved by independent `milestone_reviewer`; no Blocker or Relevant findings. The review
  also verified focused response/bridge coverage (`24 passed`).

## Outcome

- `BridgeSnapshotResponse` is frozen, rejects extra envelope fields, and requires non-blank
  request/run IDs.
- `unwrap_bridge_snapshot_response` requires exact request ID, run ID, raw payload run mode, and
  raw payload screenshot ID equality with the M-018 request before returning the exact raw payload
  mapping.
- Missing or mismatched provenance fails closed; no fallback ID is inferred.

## No-spoiler and scope

- M-021 does not sanitize, remove, normalize, or otherwise interpret raw payload fields. Forbidden
  and unknown fields remain available to the existing sanitizer, which retains rejection authority.
- `buildSnapshotResponse` reuses M-020's `buildSnapshotFromScene` and adds only request/run envelope
  metadata. It adds no model-state extraction, I/O, transport, timers, polling, or input behavior.
- Changed files: `src/fh_agent/bridge/snapshot_response.py`, `bridge/rmmv_visible_bridge.js`,
  `tests/test_bridge_snapshot_response.py`, the necessary M-019 test scoping adjustment, this
  report, the M-021 milestone, and the roadmap. No M-018 request, sanitizer, feed, observation,
  runtime, Manager, Cortex, Body, Game, CLI, canonical, dependency, live-activity, or M-022 changes
  were made.
