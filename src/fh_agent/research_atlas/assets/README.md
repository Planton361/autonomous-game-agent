# Agent Anatomy illustration source

`agent_anatomy_hero.svg` is the public-safe curated illustration input for the
generated Z2 Agent Anatomy. It contains shapes and color only. Registry identities,
technical types, links, authority boundaries, and navigation live in the Python
renderer and its generated Excalidraw metadata/hotspots.

The generator copies this exact SVG into the finite public Atlas asset path and
the finite private technical projection path. Those copies are generated outputs;
do not hand-edit them. No external image, font, script, private note, or absolute
machine path is permitted in this source asset.
